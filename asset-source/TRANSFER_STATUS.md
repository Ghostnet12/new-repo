# 4K card transfer status

- React card rendering: switched to individual `/assets/cards/00.webp` … `/assets/cards/77.webp` files.
- CSS: switched from sprite-background positioning to `<img>` card assets.
- GitHub import workflow: ready and validates exactly 78 files before committing them.
- 4K archive: prepared outside GitHub pending binary file-host transfer.
- Do not merge until `client/public/assets/cards/` contains all 78 files and CI is green.
