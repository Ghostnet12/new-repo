{
  "name": "@shadow-deck/server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/server.js",
    "start": "node src/server.js",
    "check": "node --check src/app.js && node --check src/server.js && node --check src/services/readingService.js && node --check src/tarot/deck.js",
    "test": "node --no-experimental-detect-module --no-experimental-require-module --test test/*.test.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^5.2.1",
    "express-rate-limit": "^8.7.0",
    "helmet": "^8.3.0",
    "mongoose": "^9.9.5",
    "zod": "^4.5.4"
  }
}
