import { z } from 'zod';
function validBirthday(value){if(value==='')return true;if(!/^\d{4}-\d{2}-\d{2}$/.test(value))return false;const[year,month,day]=value.split('-').map(Number);const date=new Date(Date.UTC(year,month-1,day));const now=new Date();const today=Date.UTC(now.getUTCFullYear(),now.getUTCMonth(),now.getUTCDate());return year>=1900&&date.getUTCFullYear()===year&&date.getUTCMonth()===month-1&&date.getUTCDate()===day&&date.getTime()<=today}
const birthdaySchema=z.string().max(10).refine(validBirthday,'Birthday must be a valid calendar date between 1900 and today');
const profileSchema=z.object({name:z.string().trim().max(50).optional().default(''),gender:z.string().trim().max(30).optional().default(''),birthday:birthdaySchema.optional().default(''),preferredSpread:z.enum(['three','shadow','love','career','celtic']).optional().default('three')});
export const generateSchema=z.object({persist:z.boolean().optional().default(false),profile:profileSchema.optional().default({}),question:z.string().trim().max(500).optional().default(''),spread:z.enum(['three','shadow','love','career','celtic']).default('three'),focus:z.enum(['general','love','career','decision','healing','growth']).default('general'),need:z.enum(['clarity','direction','closure','courage','understanding']).default('clarity'),reversals:z.boolean().default(true)});
export const profileUpsertSchema=profileSchema;
