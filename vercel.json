{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": "vite",
  "buildCommand": "npm run build",
  "outputDirectory": "client/dist",
  "functions": {
    "api/router.js": { "maxDuration": 30 }
  },
  "routes": [
    { "src": "/api/(?<path>.*)", "dest": "/api/router?route=$path" },
    { "handle": "filesystem" },
    { "src": "/.*", "dest": "/index.html" }
  ]
}
